import React from "react";
import PropTypes from "prop-types";
import css from "../Button/Button.module.css";

export const Button = () => {
    return <button className={css.button} type="button">Load more
    </button>
}